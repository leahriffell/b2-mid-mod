<h1>Flights</h1>

<% @flights.each do |flight| %>
  <section id="flight-<%= flight.number %>">
    <li>
      <ul>Number: <%= flight.number %></ul>
      <ul>Passengers: <%= flight.passenger_names.join(", ") %></ul>
    </li>
  </section>
<% end %>